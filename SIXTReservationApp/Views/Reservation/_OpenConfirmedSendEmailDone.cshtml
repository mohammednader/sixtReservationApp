@model SIXTReservationApp.Models.CByCustomerReservation.CustomerModel

@if (Model != null)
{
    <div class="alert alert-light d-flex flex-column align-items-center" role="alert">
        <h2 class="alert-heading">Alertify</h2>
        <p>Email was sent to @Model.Name  : @Model.Email </p>
    </div>
}
else
{
    
}